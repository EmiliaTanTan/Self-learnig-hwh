Pointer:
  指针这玩意， 说难也不难， 说简单也不简单
  一个比较重要的知识点， 就是 int* const, const int*
Demo:
  const int* p = &a; // 指针指向的值不可以更改， 指针的指向可以更改
  int* const p // 指针的指向不可以更改， 但指针指向的内存区存储的值可以变化
// 好玩： 我们可以使用指针来实现对数组的遍历

struct:
  传参时， 给参数加上const修饰， 可以有效地避免误操作
